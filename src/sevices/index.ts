import * as Logger from './logger-service';
import * as CardHolder from './card-api-client';
import Toast from 'react-native-root-toast';

export default {
  Logger,
  Api: {
    CardHolder
  },
  Toast
}
